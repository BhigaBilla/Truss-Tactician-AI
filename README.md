# Truss-Tactician-AI
An AI-driven simulation for structural truss inspection using A*, Minimax, and CSP.

## Setup
1. Clone the repo: `git clone <repo-url>`
2. Install dependencies: `pip install -r requirements.txt`
3. Run simulation: `python main.py`